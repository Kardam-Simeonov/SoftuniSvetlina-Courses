using System;

namespace RepeatString
{
    class Program
    {
        static void Main(string[] args)
        {
            string text = Console.ReadLine();
            int repeat = int.Parse(Console.ReadLine());

            repeatText(text, repeat);
        }

        static string repeatText(string text, int repeat)
        {
            
        }
    }
}
